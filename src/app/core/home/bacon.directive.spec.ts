import { BaconDirective } from './bacon.directive';

describe('BaconDirective', () => {
  it('should create an instance', () => {
    const directive = new BaconDirective();
    expect(directive).toBeTruthy();
  });
});
